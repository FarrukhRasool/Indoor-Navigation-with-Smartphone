Read `CLAUDE.md`, `docs/implementation_plan.md`, and `docs/architecture.md`.

We are developing progressively, one small milestone at a time.

Your task:
1. Determine the smallest useful next milestone (or sub-step of the current milestone) based on the implementation plan.
2. Explain why this milestone is needed and how it fits the overall pipeline.
3. List the files that would be created or touched (in `src/`, `docs/`, or `notebooks/`).
4. Define clear acceptance criteria (the Definition of Done for this step).
5. Define how the user should test it: the Python snippet or command to run (validate on Run 1 first, then all runs) and the output/plot that confirms it works.
6. Do not implement anything yet.

The milestone must be small, modular, and testable. Respect the module boundaries in `docs/architecture.md` (one responsibility per file). Do not modify the raw data (`data/raw/`) or the given assignment material (`assignment/`).

End with:
"Approve this milestone before implementation."

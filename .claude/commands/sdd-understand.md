Read `CLAUDE.md`, `assignment/Assignment.txt`, and the docs in `docs/`
(`experiment_protocol.md`, `implementation_plan.md`, `architecture.md`).

Then inspect the current project files (`src/`, `data/`, `notebooks/`).

Your task:
1. Summarize the assignment (goal, deliverable, evaluation basis).
2. Summarize the hard constraints (one filtering method, mandatory sensor fusion, door-reference evaluation, reproducible notebook, deadline).
3. Summarize the available data sources and sensors (BLE beacon RSSI, raw IMU accel/gyro/mag, filtered IMU, the four runs, door reference timestamps, floor maps).
4. Explain which lecture concepts are relevant (particle filter / Kalman filter, sensor fusion, step detection, dead reckoning).
5. Identify the current implementation status against the milestones in `docs/implementation_plan.md`.
6. Do not write or modify code.
7. Do not suggest a full final implementation yet.

End with:
"Ready for the next SDD phase."
